#pragma once
#include "Camera.h"
#include "Scene.h"
// Scene Selector
// Allows the user to dump a preset to a scene.
// id = preset ID

// Select a scene for the list
void selectScene(Scene* scene, Camera* camera, int id);